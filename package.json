{
	"name": "ep268-scorekeeper-codealong",
	"version": "1.1.0",
	"description": "",
	"scripts": {
		"start": "rm -rf dist/* && parcel src/*.html --open",
		"build": "rm -rf dist/* && parcel build src/*.html --public-url ./"
	},
	"keywords": [],
	"author": "",
	"license": "ISC",
	"devDependencies": {
		"@parcel/transformer-sass": "^2.8.3",
		"parcel": "^2.8.3",
		"sass": "^1.58.3"
	}
}
